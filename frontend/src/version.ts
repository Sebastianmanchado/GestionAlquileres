/**
 * Versión visible del sistema (Sidebar, debajo del nombre de usuario).
 * Es lo ÚNICO que se edita en cada release; ver "Versionado" en el README raíz.
 * Formato: v<MAYOR>.<MENOR 3 díg>.<PARCHE 3 díg>
 */
export const APP_VERSION = 'v1.000.001';

/** Área responsable. Texto fijo, no cambia entre versiones. */
export const APP_OWNER = 'Gcia Data & IA';

export const APP_VERSION_LABEL = `${APP_VERSION} - ${APP_OWNER}`;
